export default class Message {
    constructor(id, nickname, message, created_date) {
        this.id = id;
        this.nickname = nickname;
        this.message = message;
        this.created_date = created_date;
    }
}