export default class User {
    constructor(id, name, nickname, created_date) {
        this.id = id;
        this.name = name;
        this.nickname = nickname;
        this.created_date = created_date;
    }
}
